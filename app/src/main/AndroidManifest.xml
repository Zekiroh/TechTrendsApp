<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechTrendsApp"
        tools:targetApi="31">

        <!-- Launcher -->
        <activity
            android:name=".activities.HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.TechTrendsApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other screens -->
        <activity android:name=".activities.GenerativeAIActivity" />
        <activity android:name=".activities.HealthcareAIActivity" />
        <activity android:name=".activities.AutonomousTechActivity" />
        <activity android:name=".activities.XRActivity" />
        <activity android:name=".activities.EthicalAIActivity" />
        <activity android:name=".activities.GlossaryActivity" />

    </application>
</manifest>